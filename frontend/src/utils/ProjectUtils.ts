import { IProjectInsert } from "../interfaces/ProjectInsert";
import { IProjectReturn } from "../interfaces/ProjectReturn";

export class ProjectUtils {
  static async read(): Promise<IProjectReturn[]> {

  }

  static async insert(body: IProjectInsert): Promise<IProjectReturn> {

  }
}
